# Context

這個 repo 的共通語彙。命名、issue 標題、測試名稱、重構提案都照這裡的說法，不要漂成同義詞。

領域只有一個：**把 GitHub Issues 的阻擋關係畫成一頁讀得懂的地圖**。GitHub Issues 是唯一的事實
來源，這一頁只是它的一份快照——頁面上沒有任何東西會回寫 GitHub。

## 資料

- **快照（Snapshot）**：某一刻抓下來、算完、要交給頁面畫的全部資料。裡面只放事實與結構化的值，
  不放給人看的句子——句子換語言就要重組，存進去就得重抓一次 GitHub 才能換語言。
- **票（issue）**：一張 GitHub Issue。抓回來的原始形狀是 `RawIssue`，算完狀態之後是 `MapIssue`。
  `MapIssue` 是頁面唯一認得的形狀。
- **字彙（labels）**：這一次實際生效的角色標籤設定（`ready`／`unready`）。可以用環境變數換掉，
  所以圖例照快照裡記的寫，不照程式碼裡的預設值寫。

## 一張票的處境

- **狀態（status）**：五個值，同時是顏色、分頁與清單的排序權重。
  `ready`（現在可動）、`active`（有人接手）、`blocked`（等前置）、`triage`（規格未定案）、
  `done`（已關閉）。
- **阻擋者（blockedBy）**：全部的前置，含已經關掉的。頁面要畫出「已解鎖的前置」，所以不能在抓
  資料時就把關掉的丟掉。
- **在等誰（waitingFor）**：阻擋者裡**還開著**的那些，也就是實際的閘門。「這張票能不能動」看的
  是它，不是 `blockedBy`。
- **下一步（nextStep）**：該做什麼的**種類**（跑哪個指令、等幾張子票、等哪幾張票、有人接手…），
  不是句子。話在 i18n 那一層依當下語言組出來。
- **閘門（gate）**：狀態機這一次有沒有把 triage 標籤當成「還不能動」的條件。這個 repo 完全沒在
  用那些標籤時就不套，否則每張票都會變成待 triage。快照裡的 `labels.gated` 記的就是這一次的
  判定——頁尾照它說話，不從字彙長度重猜。

## 主票與子票

- **主票（parent）**：底下有子票的票。**主票自己不做事**，它的狀態看的是還開著的子票，不是自己
  的阻擋者；統計「幾張票」時也不算它。
- **子票**：`parent` 指向另一張票的票。關係優先讀 GitHub 原生的 sub-issue，沒有才讀內文
  `## Parent` 之後的第一個票號。
- parent 是一個指標欄位，語意上是**任意深度的森林**，不是兩層。清單與收合都要照森林處理。

## 畫面

- **群（Group）**：畫在同一張圖上的一群票，一張圖畫一群。四種：`spec`（某張主票底下的子票）、
  `orphan`（主票沒被帶進快照，只好用票號稱呼）、`linked`（沒有主票但彼此有前置關係）、
  `island`（完全互不相干）。
- **線（track）**：一條依序的前置鏈，畫成一橫。**站（station）**是線上的一張票。
- **月台（island）**：同一組裡完全沒有前置關係的票，橫排成幾列。它們不算線——十二張互不相干的
  票排成十二條單站線沒有意義。
- **關鍵路徑（criticalPath）**：最長的一條依序未完成鏈，也就是最少要幾輪才收得完。

## 刻意不採用的說法

- 不說「進度百分比」：這一頁講的是**誰擋著誰**，不是完成度。
- 不另設「短名」欄位：站名就是標題的開頭幾個字。手動維護的短名會變成票名的第二個事實來源，
  改了標題不會跟著改。
- 不把 `blockedBy` 講成「在等誰」：兩者是不同的集合，混用會讓一張其實可以動的票看起來被擋著。
- 不把「有 parent」講成「是子票就不是主票」：一張票可以同時是別人的子票與自己子票的主票。
